# API Examples
## Curl
```
curl -H 'Content-Type: application/json' \
     -d '{ "hostname": "myserver.dyname.net", "secret": "mysecret", "email": "your-email-here" }' \
     https://api.dyname.net/v1/map
```
